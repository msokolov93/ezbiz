import React from 'react';
import { createRoot } from 'react-dom/client';
import ReconciliationPage from '../frontpage.jsx';

const root = createRoot(document.getElementById('root'));
root.render(<ReconciliationPage />);
